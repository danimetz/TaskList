<h1>Your Tasks</h1>

<ol>
  <% @tasks.each do |task| %>
    <li>
      <strong><%= link_to "#{task.name}", task_path(task.id) %></strong>
    </li>
    <p>
      <%= "#{task.description} is Due: #{task.completion_date}" %>
    </p>
  <% end %>
</ol>
